import { readFileSync, writeFileSync } from 'fs';

const path = 'd:/saastest/GEO/src/app/dashboard/reports/[id]/page.tsx';
const content = readFileSync(path, 'utf8');
const lines = content.split('\n');

// Список конкретных замен строк: [lineIndex (0-based), newContent]
// Определяем по частично сломанному тексту - ищем правильный вариант
const fixes = [
  // line 70: "Рстория" → "История"
  [69, '  // История Score + план пользователя (параллельно — оба зависят только от session/report)'],
  // line 99: "Майдём" → "Найдём"
  [98, '  // Найдём предыдущий отчёт по этому проекту для кнопки "Сравнить"'],
  // line 212: "Сравнить" (Р+С выше) — выводим правильно
  // Смотрим что сейчас на этой строке
];

// Сначала выведем проблемные строки для анализа
const badLines = [70,99,143,147,151,166,212,235,238,248,251,271,272,273,277,292,293,294,295,309,314,318,326,341,342,344,345,346,347,348,360,362,369,387,388,389,390,397,399,400,408,409,410,411,418,419,420,421,439,450,453,462,466,473,489,493,500,512,612,614,638,639,661];
badLines.forEach(n => {
  console.log(`Line ${n}: ${JSON.stringify(lines[n-1])}`);
});
